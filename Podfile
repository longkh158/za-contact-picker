# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Contact Picker' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for Contact Picker
  pod 'Nimbus', '~> 1.3'

  target 'Contact PickerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Contact PickerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
